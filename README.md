# reel-separator

Split a raw video into individual reels based on a markdown script file using Whisper speech-to-text.

## Install

```bash
uv tool install .
```

## Usage

```bash
reel-separator <video_path> <markdown_path>
```

## Requirements

- `ffmpeg` and `ffprobe` on PATH
